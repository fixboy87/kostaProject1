<?xml version="1.0" encoding="UTF-8"?>


<mapper namespace="kosta.mapper.Review_BoardMapper">
	<cache />

<!-- 후기 등록 -->
	<insert id="insertReview" parameterType="Review_Board">
		insert into Review_Board(
			r_no, r_title, r_grade, r_content, r_redate, r_hitcount, userNo, h_id
		)values(
			review_seq.nextval, #{r_title}, #{r_grade}, #{r_content}, sysdate, 0,
			#{userNo}, #{h_id}
		)
	</insert>	
	
	
	
	
</mapper>