<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yanoll.mapper.HotelMapper">
	<cache />
	<select id="hotelList" resultType="Hotel">
		select h_name, h_info, pic_url, h_location from Hotel
	</select>
	
	<select id="hotelSortLocation" resultType="Hotel">
		select h_name, h_info, pic_url, h_location from Hotel where h_location = #{h_location}
	</select>
		
	
</mapper>